if(part)
{
    // No body geometry; only hole is needed for this connector style
}

if(hole)
{
    hull()
    {
        translate([-2.825, 0, 0]) cylinder(d = 2.6 + 0.4, h = 8.85);
        translate([ 2.825, 0, 0]) cylinder(d = 2.6 + 0.4, h = 8.85);
    }
    translate([0, 0, 2.5]) cylinder(r = 10, h = 100);
}
